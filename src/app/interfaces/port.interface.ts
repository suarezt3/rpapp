export interface PORT {
  created_at     : string,
  id             : string,
  inspection     : number,
  load           : number,
  portTerminal   : string,
  relocation     : number,
  storage        : number,
  totalUSD       : number,
  useInstalations: number,
  weighing       : number
}

